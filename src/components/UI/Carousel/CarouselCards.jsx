import React from 'react';
import classes from "./CarouselCards.module.css";

const CarouselCards = (props) => {
    return (
        <div className={classes.CarouselCards} {...props}>
                
        </div>
    );
};

export default CarouselCards;